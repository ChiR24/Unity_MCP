# Unity Bridge base URL
UNITY_BRIDGE_URL=http://127.0.0.1:58888

# Shared secret token (set the same value in Unity and server)
# UNITY_BRIDGE_TOKEN=changeme

# Optional debug logging for server → bridge calls
# UNITY_MCP_DEBUG=1

# Optional compile timeout in ms (default 120000)
# UNITY_COMPILE_TIMEOUT_MS=180000
